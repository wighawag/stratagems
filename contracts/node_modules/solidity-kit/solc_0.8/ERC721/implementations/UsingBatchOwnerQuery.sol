// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract UsingBatchOwnerQuery {
    // TODO access internal _ownerOf(uint256 tokenID) returns (uint256);
    // /// @notice give the list of owners for the list of ids given.
    // /// @param tokenIDs The list if token ids to check.
    // /// @return addresses The list of addresses, corresponding to the list of ids.
    // function owners(uint256[] calldata tokenIDs) external view returns (address[] memory addresses) {
    //     addresses = new address[](ids.length);
    //     for (uint256 i = 0; i < tokenIDs.length; i++) {
    //         uint256 tokenID = tokenIDs[i];
    //         addresses[i] = address(uint160(_owners[id]));
    //     }
    // }
}
