{
    "name": "missiv-service",
    "devDependencies": {
        "ldenv": "^0.3.9",
        "missiv-server": "^0.0.10",
        "set-defaults": "^0.0.2",
        "wait-on": "^7.2.0",
        "wrangler": "^3.28.4"
    },
    "scripts": {
        "prepare": "set-defaults .",
        "dev:worker": "wrangler dev -c ./wrangler.toml --port 34003",
        "dev": "pnpm run /^dev:.*/",
        "wrangler": "wrangler"
    }
}