{
  "name": "stratagems-docs",
  "version": "0.0.0",
  "description": "docs for stratagems",
  "devDependencies": {
    "ldenv": "^0.3.7",
    "vitepress": "1.0.0-rc.4"
  },
  "scripts": {
    "import:contracts:docs": "ldenv -m localhost pnpm --filter stratagems-contracts run docgen @@MODE -o ../docs/contracts @@",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  }
}